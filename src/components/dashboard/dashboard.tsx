// create a dashboard component that will be the index route
// this will be the first page the user sees
// this will display the user's health score, and a brief summary of their health status
// it will also display the user's recent activities, and a brief summary of their health metrics
// it will also display the user's recent health plans, and a brief summary of their health plans

import React from 'react';

const Dashboard: React.FC = () => {
    return <div>Dashboard</div>;
}

export default Dashboard;
